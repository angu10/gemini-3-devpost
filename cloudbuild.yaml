steps:
  # Build Docker image with API key
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg'
      - 'VITE_GEMINI_API_KEY=${_API_KEY}'
      - '-t'
      - 'gcr.io/$PROJECT_ID/smartclip-ai:$BUILD_ID'
      - '-t'
      - 'gcr.io/$PROJECT_ID/smartclip-ai:latest'
      - '.'

  # Push image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/smartclip-ai:$BUILD_ID'

  # Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'smartclip-ai'
      - '--image'
      - 'gcr.io/$PROJECT_ID/smartclip-ai:$BUILD_ID'
      - '--platform=managed'
      - '--region=us-central1'
      - '--allow-unauthenticated'
      - '--memory=512Mi'
      - '--port=8080'

# IMPORTANT: API key should be passed via command line, NOT hardcoded here
# Deploy with: gcloud builds submit --config cloudbuild.yaml --substitutions=_API_KEY=your_key_here
substitutions:
  _API_KEY: ''

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/$PROJECT_ID/smartclip-ai:$BUILD_ID'
  - 'gcr.io/$PROJECT_ID/smartclip-ai:latest'
